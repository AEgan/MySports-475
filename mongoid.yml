production:
  sessions:
    default:
      uri: <%= ENV['MONGOLAB_URI'] %>
      database: mongoid
      hosts:
        - localhost:27017
      options:
        skip_version_check: true
        safe: true

development:
  sessions:
    default:
      database: mongoid
      hosts:
        - localhost:27017

